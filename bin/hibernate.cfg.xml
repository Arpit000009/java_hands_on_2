<?xml version='1.0' encoding='utf-8'?>

<hibernate-configuration>
    <session-factory>

        <!-- DB Connection -->
        <property name="hibernate.connection.driver_class">
            org.postgresql.Driver 
        </property>

        <property name="hibernate.connection.url">
            jdbc:postgresql://localhost:5432/hibernatedb
        </property>

        <property name="hibernate.connection.username">postgres</property>
        <property name="hibernate.connection.password">IAMARPIT</property>

        <!-- Dialect -->
        <property name="hibernate.dialect">
            org.hibernate.dialect.PostgreSQLDialect  
        </property>
        <!--dialect is used as bride between hql and native sql  -->

        <!-- Automatically Create Tables -->
        <property name="hibernate.hbm2ddl.auto">update</property><!-- update,create-drop -->
        <property name="hibernate.show_sql">true</property>
        <!-- XML Mapping Files -->
        <mapping resource="Product.hbm.xml"/>
        <mapping resource="Order.hbm.xml"/>

    </session-factory>
</hibernate-configuration>